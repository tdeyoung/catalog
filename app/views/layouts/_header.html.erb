    <div class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" 
            data-target= ".navbar-collapse">
            <span class="sr-only">Toggle Navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <div id: "logo">
          <%= link_to "ScoreShare", root_path, id: "logo" %>
          </div>

          <% if logged_in? and current_user.district_id != nil %>
            <span class="panel">
              <%= current_user.district.title %>
            </span>
          <% end %>

        </div> <!-- end div class="navbar-header" -->
          <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
              <% if !logged_in? %>
              <li><%= link_to "Log in",    login_path %></li>
              <% end %>
              <li><%= link_to "Help",    help_path %></li>
              <% if logged_in? %>
              <li><%= link_to "Show All Music",   items_path %></li>
              <li><%= link_to "Search",  search_path %></li>
              <li><%= link_to "Members", users_path %></li>
              <li ><%= link_to "Home",   root_path %></li>
              <li><%= link_to "Logout",  logout_path %></li>
              <% end %>
              
            </ul>
          </div> <!-- end div class="collapse navbar-collapse" -->
        </div> <!-- end div class = "container" -->
      </div> <!-- end div class="navbar navbar-fised-top navbar-inverse" -->